# Next.js SEO SPA (Option A)

This project is a starter template for:
- Next.js (App Router) + TypeScript
- Tailwind CSS
- PWA (manifest, service worker, offline)
- SEO-ready multi pages + SPA dashboard
- Google Apps Script backend + Google Sheets DB

See `.env.local.example` for environment variables.
